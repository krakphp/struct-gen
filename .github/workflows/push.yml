name: PHP Tests

on:
  push:
    branches:
    - v0.x 
  pull_request:
    branches:
    - v0.x

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: composer install
      - name: Run Tests
        run: composer test
